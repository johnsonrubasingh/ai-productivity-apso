global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: apso-backend
    metrics_path: /api/v1/ops/metrics
    static_configs:
      - targets:
          - backend:8000

  - job_name: prometheus
    static_configs:
      - targets:
          - prometheus:9090
